<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="ButtonDefaultStyle" TargetType="Button">
        <Setter Property="Padding" Value="12,3"/>
        <Setter Property="Background" Value="{StaticResource ButtonDefaultBgColorRestBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonDefaultBorderColorRestBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonDefaultFgColorRestBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border TextBlock.Foreground="{TemplateBinding Foreground}" x:Name="Border" CornerRadius="6" 
                            Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter Margin="2" x:Name="ContentPresenter" 
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                          RecognizesAccessKey="True" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                To="{StaticResource ButtonDefaultBgColorHover}"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                To="{StaticResource ButtonDefaultBorderColorHover}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                To="{StaticResource ButtonDefaultBgColorActive}"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                To="{StaticResource ButtonDefaultBorderColorActive}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                To="{StaticResource ButtonDefaultBgColorDisabled}"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                To="{StaticResource ButtonDefaultBorderColorDisabled}"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(Panel.Opacity)"
                                                Storyboard.TargetName="ContentPresenter" Duration="0:0:0.04"
                                                To="0.3"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Button" BasedOn="{StaticResource ButtonDefaultStyle}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
    </Style>
</ResourceDictionary>