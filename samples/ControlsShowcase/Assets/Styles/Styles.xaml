<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Code Display Style -->
    <Style x:Key="CodeTextBoxStyle" TargetType="TextBox">
        <Setter Property="FontFamily" Value="Consolas, Courier New, monospace" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Background" Value="{DynamicResource BgColorInsetBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource FgColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
    </Style>

    <!-- Expander Header Style -->
    <Style x:Key="SampleExpanderStyle" TargetType="Expander">
        <Setter Property="Background" Value="{DynamicResource BgColorMutedBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Margin" Value="0,0,0,16" />
    </Style>
</ResourceDictionary>